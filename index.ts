/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MeetingSDK from './src/MeetingSDK';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MeetingSDK);
