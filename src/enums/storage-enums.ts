
export enum StorageEnums {
  IceServers = "iceServers",
  redirect = "redirect",
  lockRequests = "token_key_lock",
  loginToken = "login_token",
  token = "token",
  userEndCallRedirectUrl = "user_end_call_redirect_url",
}
